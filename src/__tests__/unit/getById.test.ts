import { jest, describe, it, expect, beforeEach } from "@jest/globals";

describe("GetById Owner Use Case", () => {
  it("should return owner when found", async () => {
    // Por ahora solo verificamos que Jest funciona
    expect(1 + 1).toBe(2);
  });
});
